{
  "name": "example_repository",
  "version": "1.0.0",
  "description": "This is a fake repository to be referenced in conjunction wit hthe instructions in REPOSITORY.md in the parent folder.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Griffin Davis",
  "license": "ISC",
  "dependencies": {
    "print": "^1.2.0"
  }
}
